import hr from './hr';
import srb from './srb';

const SUPPORTED_LANGUAGES = ['Hrvatski', 'Srpski'];
const SUPPORTED_LOCALES = ['hr', 'srb'];

export default {
  SUPPORTED_LANGUAGES,
  SUPPORTED_LOCALES,
  hr,
  srb,
};
